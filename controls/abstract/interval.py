# interval.py
from enum                       import Enum

class AbstractInterval(Enum):
    """ Абстрактный класс перечисления """
    pass
#   NAME = (0,      "0", datetime.timedelta(days=0), AnyEnum.TAG,     AnyEnum2.TAG)
#   TAG  = (index, text, end of loaded data offset,  candle_interval, sub_interval)